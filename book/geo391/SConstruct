from rsfbook import *

papers = map(lambda x: 'hw%d' % x,range(1,7))

Book(title='\\large{GEO391} \\ Multidimensional Data Analysis in Geosciences \\\\ (Homeworks)',
     author='Sergey Fomel',
     chapters=papers)

End(options='book',use='hyperref,listings,newalg')
