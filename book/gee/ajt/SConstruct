from rsfproj import *
##################################################################
import glob

dirs = filter(os.path.isdir,glob.glob('[a-z]*'))

for dir in dirs:
    Dir(dir)
    SConscript(os.path.join(dir,'SConstruct'))

    
##################################################################
End()
