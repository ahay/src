INPUT DATA FOR iwave

------------------------------------------------------------------------
FD:

         order = 2
           cfl = 0.4
          cmin = 1.0
          cmax = 4.5
	  dmax = 5000
	  dmin = 500
         fpeak = 0.010		peak frequency

	 ansol = HI		analytical solution flag

------------------------------------------------------------------------
Model info:

        lambda = test7_lambda.rsf
            mu = test7_mu.rsf
	density = test7_dn.rsf
       buoyancy = test7_buoy.rsf
      velocity = test7_vp.rsf


------------------------------------------------------------------------
Source info:

      srctype = point
 waveletphase = causal
      sampord = 1             	sampling order for source
       refamp = 1
        srcin = body		source injection type

------------------------------------------------------------------------
Trace info:

      hdrfile = hdr_short.su
     datafile = p0.su
    datafile1 = p0 		z pressure
    datafile2 = p1		x pressure
    datafile3 = v0		z velocity
    datafile4 = v1		v velocity
    datafile5 = s0		zx stress 

------------------------------------------------------------------------
Movie info:

    moviestep = 10.0         time between movie frames (<= 0.0, 
                             or do not include, for no movie)
       movie1 = p0
       movie2 = ansol_p0

------------------------------------------------------------------------
PML info:

          nl1 = 0.5         z - neg
          nr1 = 0.5         z - pos
          nl2 = 0.5         x - neg
          nr2 = 0.5         x - pos

MPI info:

      mpi_np1 = 1           n_doms along axis 1
      mpi_np2 = 1           n_doms along axis 2
      mpi_np3 = 1           n_doms along axis 3
      partask = 1

Output info:

     printact = 3
      dump_pi = 0           dump parallel/dom. decomp info
     dump_lda = 0           dump grid data for allocated arrays
     dump_ldc = 1           dump grid data for computational arrays
    dump_term = 1           dump terminator data
 dump_wavelet = 1	    durm wavelet generated
