/* The Faddeeva.cc file contains macros to let it compile as C code
   (assuming C99 complex-number support), so just #include it. */
#include <rsf.h>

#ifdef SF_HAS_COMPLEX_H  
#include "Faddeeva3.cc"
#endif
