import sys, os
sys.path.append('../..')
import configure

try: # distribution version
    Import('env root libdir incdir')
    env = env.Clone()
except: # local version
    env = configure.Debug()
    root = None
    SConscript('../c/SConstruct')

env.Prepend(CPPPATH=['../../include'],
            LIBPATH=['../../lib'],
            LIBS=['rsf'])

if 'f77' in env.get('API',[]):
    F77 = env.get('F77')
    F77base =  os.path.basename(F77)

    env.Append(CCFLAGS=' -D$CFORTRAN')

    if F77base[:8] == 'gfortran' or F77base[:3] == 'gfc':
        env.Append(CCFLAGS=' -DGFORTRAN')
        
    env.Object('fortran.o','fortran.c')

    lib = env.StaticLibrary('rsff','fortran.o')
    env.Install('../../lib',lib)
    
    if root:
        env.Install(libdir,lib)

    for file in ['getpar','file']:
        test = 'Test' + file + '.f'
        env.Program(file,[test,lib],
                    PROGPREFIX='Test',
                    PROGSUFFIX='.x')

        
